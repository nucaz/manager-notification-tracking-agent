<%- include('../partials/head', { title: title }) %>

<div class="d-flex justify-content-between align-items-start mb-3">
  <div>
    <h2 class="mb-0"><%= item.name %></h2>
    <span class="badge <%= item.criticality === 'critica' ? 'bg-danger' : item.criticality === 'alta' ? 'bg-warning text-dark' : item.criticality === 'media' ? 'bg-info text-dark' : 'bg-secondary' %>">
      Criticidad: <%= item.criticality %>
    </span>
    <span class="badge <%= item.computed_status === 'vencido' ? 'bg-danger' : item.computed_status === 'por_vencer' ? 'bg-warning text-dark' : item.computed_status === 'activo' ? 'bg-success' : 'bg-secondary' %>">
      Soporte: <%= item.days_left !== null ? item.days_left + ' día(s) restantes' : 'sin fecha' %>
    </span>
    <span class="badge bg-secondary text-uppercase">Estado: <%= item.status %></span>
  </div>
  <div>
    <% if (currentUser.role !== 'lector') { %>
      <a href="/servidores/<%= item.id %>/editar" class="btn btn-outline-primary">Editar</a>
      <form action="/glpi/sincronizar/server/<%= item.id %>" method="post" class="d-inline">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <button class="btn btn-outline-secondary" type="submit" title="Crear/actualizar como Contrato en GLPI">
          <i class="bi bi-cloud-upload"></i> Sincronizar con GLPI
        </button>
      </form>
      <form action="/servidores/<%= item.id %>/eliminar" method="post" class="d-inline"
            onsubmit="return confirm('¿Eliminar este activo? Esta acción no se puede deshacer.');">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <button class="btn btn-outline-danger" type="submit">Eliminar</button>
      </form>
    <% } %>
  </div>
</div>

<div class="row g-3">
  <div class="col-md-7">
    <div class="card p-3">
      <h5>Detalle</h5>
      <table class="table table-sm">
        <tr><th style="width:220px;">Tipo de activo</th><td><%= item.asset_type %></td></tr>
        <tr><th>Ambiente</th><td><%= item.environment %></td></tr>
        <tr><th>Dirección IP</th><td><%= item.ip_address || '—' %></td></tr>
        <tr><th>Sistema operativo</th><td><%= item.operating_system || '—' %></td></tr>
        <tr><th>Proveedor / fabricante</th><td><%= item.provider || '—' %></td></tr>
        <tr><th>Responsable</th><td><%= item.responsible || '—' %></td></tr>
        <tr><th>Local / sede / datacenter</th><td><%= item.site_location || '—' %></td></tr>
        <tr><th>Fecha de compra</th><td><%= item.purchase_date || '—' %></td></tr>
        <tr><th>Vencimiento de soporte/garantía</th><td><%= item.support_expiration_date || '—' %></td></tr>
        <tr><th>Costo</th><td>
          <%= item.cost ? item.cost + ' ' + item.currency : '—' %>
          <% if (item.cost && item.currency === 'USD' && exchangeRate) { %>
            <span class="text-muted small">(≈ S/ <%= (item.cost * exchangeRate.rate).toFixed(2) %>, BCRP <%= exchangeRate.date.split('-').reverse().slice(0,2).join('/') %>)</span>
          <% } %>
        </td></tr>
        <tr><th>Equipo GLPI vinculado</th><td><%= item.glpi_computer_id || '—' %></td></tr>
        <tr><th>Contrato GLPI sincronizado</th><td><%= item.glpi_contract_id || '—' %></td></tr>
        <tr><th>Dependencias</th><td><%= item.dependencies || '—' %></td></tr>
        <tr><th>Notas</th><td><%= item.notes || '—' %></td></tr>
      </table>
    </div>
  </div>

  <div class="col-md-5">
    <div class="card p-3">
      <h5>Adjuntos (contratos, facturas, fichas técnicas)</h5>
      <% if (currentUser.role !== 'lector') { %>
        <form action="/adjuntos/subir/server/<%= item.id %>" method="post" enctype="multipart/form-data" class="mb-3 border-bottom pb-3">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <div class="mb-2">
            <select name="doc_type" class="form-select form-select-sm">
              <option value="contrato">Contrato</option>
              <option value="adenda">Adenda</option>
              <option value="factura">Factura</option>
              <option value="otro">Otro</option>
            </select>
          </div>
          <div class="mb-2">
            <input type="text" name="description" class="form-control form-control-sm" placeholder="Descripción (opcional)">
          </div>
          <div class="mb-2">
            <input type="file" name="file" class="form-control form-control-sm" required>
          </div>
          <button type="submit" class="btn btn-sm btn-primary">Subir archivo</button>
        </form>
      <% } %>
      <ul class="list-group list-group-flush">
        <% attachments.forEach(function(a) { %>
          <%- include('../partials/attachment_item', { a: a }) %>
        <% }); %>
        <% if (attachments.length === 0) { %>
          <li class="list-group-item px-0 text-muted">Sin archivos adjuntos.</li>
        <% } %>
      </ul>
    </div>
  </div>
</div>

<%- include('../partials/foot') %>
