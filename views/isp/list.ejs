<%- include('../partials/head', { title: title }) %>

<div class="d-flex justify-content-between align-items-center mb-3">
  <h2 class="mb-0">Contratos ISP</h2>
  <% if (currentUser.role !== 'lector') { %>
    <div>
      <a href="/isp/importar" class="btn btn-outline-secondary"><i class="bi bi-upload"></i> Importar</a>
      <a href="/isp/nuevo" class="btn btn-primary"><i class="bi bi-plus-lg"></i> Nuevo contrato</a>
    </div>
  <% } %>
</div>

<form class="row g-2 mb-3" method="get">
  <div class="col-auto">
    <input type="text" name="q" value="<%= q %>" class="form-control" placeholder="Buscar por proveedor, N° de contrato o servicio...">
  </div>
  <div class="col-auto">
    <select name="status" class="form-select">
      <option value="">Todos los estados</option>
      <option value="vencido" <%= status === 'vencido' ? 'selected' : '' %>>Vencidos</option>
      <option value="por_vencer" <%= status === 'por_vencer' ? 'selected' : '' %>>Por vencer</option>
      <option value="activo" <%= status === 'activo' ? 'selected' : '' %>>Activos</option>
    </select>
  </div>
  <div class="col-auto">
    <button class="btn btn-outline-secondary" type="submit">Filtrar</button>
  </div>
</form>

<div class="card p-3">
  <div class="table-responsive">
    <table class="table table-hover align-middle">
      <thead>
        <tr>
          <th>Proveedor</th><th>N° contrato</th><th>Servicio</th><th>Ancho de banda</th>
          <th>Costo mensual</th><th>Vence</th><th>Estado</th><th></th>
        </tr>
      </thead>
      <tbody>
        <% items.forEach(function(item) { %>
          <tr>
            <td><a href="/isp/<%= item.id %>"><%= item.provider %></a></td>
            <td><%= item.contract_number || '—' %></td>
            <td><%= item.service_type || '—' %></td>
            <td><%= (item.bandwidth_down || '—') + ' / ' + (item.bandwidth_up || '—') %></td>
            <td><%= item.monthly_cost ? item.monthly_cost + ' ' + item.currency : '—' %></td>
            <td><%= item.end_date || '—' %></td>
            <td>
              <span class="badge <%= item.computed_status === 'vencido' ? 'bg-danger' : item.computed_status === 'por_vencer' ? 'bg-warning text-dark' : item.computed_status === 'activo' ? 'bg-success' : 'bg-secondary' %>">
                <%= item.days_left !== null ? item.days_left + ' día(s)' : 'sin fecha' %>
              </span>
            </td>
            <td class="text-end">
              <a href="/isp/<%= item.id %>" class="btn btn-sm btn-outline-secondary">Ver</a>
              <% if (currentUser.role !== 'lector') { %>
                <a href="/isp/<%= item.id %>/editar" class="btn btn-sm btn-outline-primary">Editar</a>
              <% } %>
            </td>
          </tr>
        <% }); %>
        <% if (items.length === 0) { %>
          <tr><td colspan="8" class="text-center text-muted py-4">No hay contratos ISP registrados.</td></tr>
        <% } %>
      </tbody>
    </table>
  </div>
</div>

<%- include('../partials/foot') %>
